services:
  - type: web
    name: recipe-maker-backend
    env: python
    plan: free
    rootDir: backend
    pythonVersion: 3.10.18
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
